# Elastic
Benchmarking with the elastic api in python

First install from the elasticsearch-py package from https://github.com/elastic/elasticsearch-py

functions.py: the class boxFetcher generates a bounding box around a random position in a larger bounding box

For plotting install matplotlib
